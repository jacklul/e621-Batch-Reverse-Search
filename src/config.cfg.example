; e621 Batch Reverse Search Script
; Copyright (c) 2016 Jack'lul <https://jacklul.com>
;
; Config file
;  Uncomment options you want to change (remove ";" before it)
;  Try to use absolute paths!
;

; Set debug mode on or off (default: false)
;  Enabling it will print some additional information in the console while script is running
;DEBUG=true

; Path to images directory (default: 'images' folder in script directory)
;PATH_IMAGES="/home/jacklul/e621/images/"

; Log console output to a file or not (default: false)
;  Enabling it will write all console output to a file
;LOGGING=true

; Path to logs directory (default: 'logs' folder in script directory)
;PATH_LOGS="/home/jacklul/e621/"

; Output links to HTML file or not (default: true)
;  When enabled all found links are pushed into HTML file
;OUTPUT_HTML=true

; Output to HTML destination file (default: 'links.html' file in 'images/found' folder in script directory)
;OUTPUT_HTML_FILE="/home/jacklul/e621/links.html"

; Whenever to do MD5 search or not (default: true)
;MD5_SEARCH=true

; Whenever to do batch MD5 searches before everything else (default: true)
; This prefetches all MD5 searches by doing batch queries to the API
;MD5_BATCH_SEARCH=true

; Whenever to do Reverse search or not (default: true)
;REVERSE_SEARCH=true

; Use php-wfio extension or not [Windows only] (default: true)
;USE_PHPWFIO=true

; Convert image into JPG with '90' quality before uploading (default: true)
;  It should not affect the results but can help when a lot of images
;  are big in size (3MB or more) and fail to upload
;USE_CONVERSION=true

; Your e621 username
; Required for the e621 IQDB to work properly
E621_LOGIN=

; Your e621 API KEY (Account -> Manage API Access)
; Required for the e621 IQDB to work properly
E621_API_KEY=

; Use additional services for reverse search (default: true)
;USE_MULTI_SEARCH=true

; Forces searching on all services even when links are already found (default: false)
;FORCE_MULTI_SEARCH=false

; API key for 'saucenao.com' service (default: '')
;SAUCENAO_API_KEY=

; Whenever to search all image sites supported by 'saucenao.com' (default: false)
;  By default only e621 is searched
;SAUCENAO_SEARCH_ALL=false

; API key for 'fuzzysearch.net' service (default: '')
;FUZZYSEARCH_API_KEY=

; Maximum time the script waits for reply per image search (default: 60)
;  Set it to higher number on weaker connections or errors like 'no reply from the server'
;RETURN_TIMEOUT=60
